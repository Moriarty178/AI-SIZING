.idea/
*.log

# Gói chuyển file giữa các máy — không commit (docs.zip ~21MB, chứa cả venv)
*.zip

# Môi trường ảo tạm khi trích text PDF
**/.tmp/venv/
.venv/
__pycache__/

# Giữ lại docs/rules/.tmp/clean.txt và raw-layout.txt — đó là nguồn trích dẫn
# nguyên văn cho quy tắc định tính (NT2), phải commit.

# cấu hình có thể chứa endpoint nội bộ; khóa API nằm trong biến môi trường
config/settings.yaml

# Mẫu Word sinh lại được bằng scripts/make_word_template.py — không commit bản sinh
data/knowledge_base/mau-sizing-chuan.docx

# Kết quả 1.17 sinh lại được bằng scripts/fill_checklist.py — không commit
docs/checklist/
.cache/

# Báo cáo DIỄN TẬP (model giả) — số vô nghĩa về chất lượng, giữ lại chỉ gây nhầm
# với báo cáo eval thật. Sinh lại bất cứ lúc nào bằng `run_eval --gia-lap`.
eval/reports/dien-tap-*.md
