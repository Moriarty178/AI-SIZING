# 5.0b — đo độ ổn định giữa hai lượt thẩm định

> ✅ **Phép đo dùng được.**

> - Lượt A: 268 lời gọi model. ✓
> - Lượt A: đệm TẮT → không lời gọi nào được phát lại.
> - Lượt B: 272 lời gọi model. ✓
> - Lượt B: đệm TẮT → không lời gọi nào được phát lại.

Đo lúc 2026-09-16 18:06. Cùng một tài liệu, **không sửa gì**.

| | Lượt A | Lượt B |
|---|---|---|
| Mã việc | `36d2314d3826` | `790a8dd95594` |
| Tài liệu | `Thiet ke va dinh co he thong_VTracking 2.0.1.docx` | `Thiet ke va dinh co he thong_VTracking 2.0.1.docx` |
| Tuỳ chọn | `{'song_song': 12}` | `{'song_song': 12}` |
| Số finding | 717 | 718 |
| Phút chạy | 21.7 | 22.6 |
| **Lời gọi model** | 268 | 272 |
| Đệm | `{'bat': False, 'ban_ghi_truoc': 196, 'ban_ghi_sau': 196, 'ghi_them': 0}` | `{'bat': False, 'ban_ghi_truoc': 196, 'ban_ghi_sau': 196, 'ghi_them': 0}` |

## Khoá nối giữa hai lượt  → quyết định 5.1

| Số đo | Giá trị | Tỉ lệ |
|---|---|---|
| Khớp theo `finding_id` | 658 | 91,6% |
| Chỉ có ở lượt A | 59 | 8,2% |
| Chỉ có ở lượt B | 60 | 8,4% |
| Khớp khi BỎ hậu tố `#N` | 658 | 91,6% |
| → mất khớp CHỈ vì hậu tố thứ tự | **0** | 0,0% |
| Khớp khi CHUẨN HOÁ tên phân hệ (bỏ phần trong ngoặc) | 708 | 98,6% |
| → mất khớp CHỈ vì C3 diễn đạt lại tên phân hệ | **50** | 7,0% |

Dòng cuối là lỗi khoá, sửa được bằng code (`report.py` gắn hậu tố theo thứ tự duyệt). Phần còn lại là model thật sự đổi ý — không sửa bằng code được.

## Trong số dòng khớp, có gì đổi  → quyết định 5.6

| Đổi | Số dòng | Tỉ lệ trên số khớp |
|---|---|---|
| Mức độ (severity) | 24 | 3,6% |
| Nhóm (vòng / chưa kiểm được) | 5 | 0,8% |
| `computed_evidence` (con số code tính) | 5 | 0,8% |
| Vị trí | 17 | 2,6% |
| Câu chữ mô tả | 61 | 9,3% |

**Bền hoàn toàn** (khớp id, không đổi mức độ / nhóm / căn cứ): **632** dòng — 88,0% của lượt lớn hơn.

Đổi *câu chữ* không đáng lo: bảng 5.6 khoá theo `finding_id`, không theo câu. Đổi *mức độ*, *nhóm* hay *căn cứ* mới đáng lo — đó là những thứ Admin đọc.

## Bao nhiêu dòng code KẾT LUẬN được  → cột Trạng thái của 5.6

| Số đo | Lượt A | Lượt B |
|---|---|---|
| Có `computed_evidence` (C4 tính lại được) | 10 | 11 |
| …trong đó khớp cả hai lượt | 8 | |
| Xếp nhóm «chưa kiểm được» | 624 | |

Chỉ 1,4% số finding có con số do code tính — chỉ những dòng đó mới có thể mang trạng thái *Đạt* / *Chưa đạt* một cách chắc chắn. Phần còn lại là *Chưa kiểm được*, và đó là sự thật cần cho Admin thấy chứ không phải chỗ để đoán.

## Đọc số này để quyết định gì

- Khớp `finding_id` **≥ 95%** → 5.1 dùng thẳng `finding_id` làm khoá baseline.
- **85–95%** → phải vá hậu tố `#N` và neo `scope_key` trước khi viết 5.1.
- **< 85%** → khoá theo `(rule_ref, vị trí trong tài liệu)` chứ không theo `scope_key` do C3 trích; và 5.3 phải coi «finding biến mất» là *chưa kết luận* chứ không phải *đã sửa xong*.
- Dòng đổi mức độ/nhóm **> 10%** số khớp → cột Trạng thái của 5.6 bắt buộc phải hiện cả «lượt nào kết luận», không chỉ kết luận cuối.

## Lượt nào gọi model thật

Lượt A chạy 21.7 phút, lượt B 22.6 phút. Một tài liệu gọi model thật tốn **~16–23 phút** (đo 2026-09-16: 21,7 và 22,6 phút, 268 và 272 lời gọi); xong trong vài phút nghĩa là lượt đó lấy từ đệm hoặc lời gọi đang hỏng.

Lượt A lấy từ đệm thì KHÔNG sao — nó tái hiện đúng đầu ra model của lượt gốc. **Lượt B lấy từ đệm mới là hỏng**: khi đó nó chỉ phát lại lượt A và mọi tỉ lệ trên đây đều là 100% giả.

## Cảnh báo về chính phép đo

- Script KHÔNG kiểm được hai lượt có chạy cùng một commit hay không — kho việc không lưu phiên bản mã theo từng việc. Tự đối chiếu trước khi tin con số.
- Nếu lượt B chạy khi đệm lời gọi còn bật, nó chỉ phát lại lượt A và mọi tỉ lệ trên đây sẽ là 100%. Con số 100% ở đây nên bị nghi ngờ trước khi được mừng.

## Ví dụ finding chỉ có ở một lượt

Chỉ ở A: `ARC-02#Master (K8s Master node)`, `ARC-03#Master (K8s Master node)`, `ARC-06#MinIO`, `ARC-08#Master (K8s Master node)`, `ARC-09#Master (K8s Master node)`, `ARC-11#Master (K8s Master node)`, `ARC-14#Master (K8s Master node)`, `ARC-15#Master (K8s Master node)`, `ARC-16#Master (K8s Master node)`, `ARC-19#Master (K8s Master node)`

Chỉ ở B: `ARC-02#Master (K8s Control plane)`, `ARC-03#Master (K8s Control plane)`, `ARC-06#FrontEnd`, `ARC-06#Master (K8s Control plane)`, `ARC-08#Master (K8s Control plane)`, `ARC-09#Master (K8s Control plane)`, `ARC-11#FrontEnd`, `ARC-11#Master (K8s Control plane)`, `ARC-14#Master (K8s Control plane)`, `ARC-14#MinIO`
